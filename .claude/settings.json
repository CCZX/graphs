{
	"hooks": {
		"PostToolUse": [
			{
				"matcher": "Write|Edit",
				"hooks": [
					{
						"type": "command",
						"command": "jq -r '.tool_input.file_path' | grep -E '\\.(ts|tsx)$' | { read -r f; [ -n \"$f\" ] && npx eslint \"$f\" --fix; } 2>/dev/null || true"
					}
				]
			}
		]
	}
}
